# booking/requirements.txt
# === PilatesHQ WhatsApp Bot – Google Sheets Edition ===
# Clean version with no database dependencies.

Flask==3.0.3
gunicorn==22.0.0
requests==2.32.3
python-dotenv>=1.0.1

# Optional (keep only if used elsewhere)
reportlab>=4.0.4      # For PDF generation
openai>=1.0.0         # For GPT-based messaging or analysis
openpyxl==3.1.2       # For reading/writing Excel sheets